Implementation of Perceptron in Python from scratch (using NumPy) <br/>
Tested it on 2 datasets
- Linearly separable data
- Non linearly separable data <br/>

Visualized the decision boundary on both datasets
